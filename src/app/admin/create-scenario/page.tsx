'use client';

// Update the import path if the component is located elsewhere, for example:
import { CreateScenarioBase } from './_components/CreateScenarioBase';
// Or, if the file extension is needed:
// import { CreateScenarioBase } from '../_components/CreateScenarioBase.tsx';

export default function Page() {
  return <CreateScenarioBase />;
}
